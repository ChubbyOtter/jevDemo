# Runtime grounding + assertion judgments (https://console.typesafe.ai/keys)
TYPESAFE_API_KEY=

# One-time "compile" pass that turns each NL step into a typed record (cached after first run)
ANTHROPIC_API_KEY=
